# Optional Extras

Things that are *not* auto-installed, because they're either too personal, too
situational, or only useful to some. You go get them yourself if you want them.

> This list is deliberately empty at 0.1.0. It fills as the owner decides what earns
> the `core` label vs what stays here as a "go get it yourself" link.
